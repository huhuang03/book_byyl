为下面的各个语言构造无二义性的上下文无关文法。证明你的文法都是正确的。

1) 用后缀方式表示的算数表达式
S -> SS-|SS+|SS*|SS/|a

2) 由逗号分隔开的左结合的标示符列表
S2 = S1,a
S1 = a,a|a
a为标示符

3) 逗号分隔开的右结合的标示符列表
S2 = a,S1
S1 = a,a|a

4) 由整数、标示符、四个二目运算符+、-、*、/构成的算数表达式。

d表示整数t标示符
op -> d|t
S1 -> op*op|op/op|op
S2 -> S1+S1|S1-S1|S1

5) 在4)的运算符中增加单目+和单目-构成的算数表达式。
op -> d|5
op1 -> -d|+d
S1 -> op1*op1|op1/op1|op1
S2 -> S1+S1|S1-S1|S1
   
同理，证明我就没有这个能力了。
